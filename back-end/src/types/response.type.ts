/**
 * This interface is used to define the structure of the response object
 * that will be sent to the client when a confirmation message is needed.
 */
export interface IConfirmationMessage {
  message: string;
}
