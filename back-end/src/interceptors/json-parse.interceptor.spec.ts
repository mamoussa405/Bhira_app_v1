import { JsonParseInterceptor } from './json-parse.interceptor';

describe('JsonParseInterceptor', () => {
  it('should be defined', () => {
    expect(new JsonParseInterceptor()).toBeDefined();
  });
});
